bundle:
  name: demo_project

include:
  - resources/only_dev/*.yml
  - resources/shared/*.yml

variables:
  environment: 
    default: ${bundle.target}
  name_suffix:
    description: "Target specific suffix for the job name"

targets:
  dev:  
    default: true
    workspace:
      host:   
  

  staging:
    workspace:
        host: 
  

  prod:
    workspace:
        host: 
   
